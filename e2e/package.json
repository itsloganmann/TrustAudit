{
  "name": "trustaudit-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end tests for TrustAudit Tax Shield. Boots the FastAPI backend (which serves the built React bundle) and exercises the public surface area: landing page, /live, /help/demo, vendor signin, and the WhatsApp inbound webhook.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "PWDEBUG=1 playwright test",
    "report": "playwright show-report",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
