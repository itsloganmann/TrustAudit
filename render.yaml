# Render.com Blueprint — Single Web Service (Docker)
# Uses Dockerfile that bundles Node.js + Python for reliable builds
services:
  - type: web
    name: trustaudit
    runtime: docker
    plan: free
    healthCheckPath: /health

