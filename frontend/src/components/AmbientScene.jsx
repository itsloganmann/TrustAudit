/**
 * AmbientScene — neutralized on the light theme.
 *
 * Previously a WebGL backdrop that drifted a subtle liquid gradient
 * over the dark theme. The product surface is now white, so we
 * render nothing and keep the default export so existing imports
 * continue to work.
 */
export default function AmbientScene() {
  return null;
}
