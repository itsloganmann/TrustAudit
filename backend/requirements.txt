fastapi>=0.115.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.49
# Postgres driver (Render Postgres add-on) + migrations
psycopg[binary]>=3.2
alembic>=1.13
pydantic>=2.5.3
# EmailStr validator for the pilot intake form (POST /api/pilot/applications)
email-validator>=2.2.0
python-multipart>=0.0.18
Pillow>=10.2.0
httpx>=0.26.0
# Auth + identity (added by manager from W1 + W5 + W6 requests)
# NOTE: W5 uses `bcrypt` directly (not via passlib) because passlib 1.7.4
# and bcrypt 5.0.0 have a known incompatibility. passlib[bcrypt] is kept
# for seed.py's fallback path.
bcrypt>=4.1.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
PyJWT>=2.9.0
authlib>=1.3.0
# Real-time push
sse-starlette>=2.1.0
# QR codes for landing page + compliance form
qrcode[pil]>=7.4.0
# PDF generation for compliance reports + dispute filings (W9).
# Requires system libs: cairo, pango, gdk-pixbuf, libffi, fonts-dejavu-core
# (installed by Dockerfile stage 3).
weasyprint>=62.0
# Dev / test
pytest>=8.0.0
